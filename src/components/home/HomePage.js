import React, { PropTypes } from 'react';
import { Link, IndexLink } from 'react-router';

const HomePage = () =>
  (
    <p>You stumbled onto the awesome app Nevin is making, make sure you take a peek inside! </p>
  );

export default HomePage;
